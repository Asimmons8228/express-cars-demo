<%- include('../partials/head') %>

<h1>All Cars</h1>
<ul>
    <% cars.forEach(car=>{ %>
    <h1><%= car.make%><h1>
    <li>Year: <%= car.year%></li>
    <li>Used: <%= car.isUsed ? "Yes" : "No" %></li>
    <% }) %>
</ul>
<a href="/">Go Back</a>
<%- include('../partials/footer') %>
